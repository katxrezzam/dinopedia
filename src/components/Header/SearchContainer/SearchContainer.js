import './SearchContainer.scss'

const SearchContainer = ({result}) => {
    return(
        <div className="search-result">
            {result}
        </div>
    )
}
export default SearchContainer
